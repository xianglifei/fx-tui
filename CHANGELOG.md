# Changelog

本项目的所有显著变更记录于此。版本格式遵循 [SemVer](https://semver.org/)，
条目参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.20.4] - 2026-08-31

### 性能：会话重放批量提交 — 长会话恢复/切换从 O(N²) 快照重建到单次提交

- **动机**：`replay()` 逐事件走 `onEvent`，而 `onEvent` 末尾无条件 `commit()`——
  每次提交都复制整个条目数组（`[...items]`），恢复 N 个事件就是 O(N²) 拷贝。
  启动重放时尚无订阅者只是白算；`/sessions` 切换会话时 React 已订阅，几千事件
  的会话会连续触发数千次快照重建
- **修复**：`onEvent` 在重放期间跳过 `commit()`，由 `finishReplay()` 统一在
  重放结束时提交一次。两条调用路径（启动 `index.ts:235-236`、会话切换
  `reset()` 内部）本就以 `finishReplay()` 收尾，行为不变
- **影响面**：重放期间不再产生中间快照（重放本就不该有过渡帧）；活跃事件流
  逐事件提交的节奏不变。新增 `store.test.ts` 用订阅计数锁住三个约定：
  重放 0 次提交、`finishReplay` 恰好 1 次、重放后活跃事件立即提交
- **顺手修复**：`tsconfig.test.json` 配置内声明的 `noEmit` 会被
  typescript 7.0.2（native）忽略——`pnpm typecheck` 第二段 tsc 因此把整个
  `lib/` 连同编译后的 `*.test.js/.d.ts` 全量重发，vitest 双份跑测试、npm
  `files` 产物被污染。typecheck 脚本对两段 tsc 都显式传 CLI `--noEmit`
  （实测旗标有效、配置无效），已清理 lib 残留

## [0.20.3] - 2026-08-31

### 测试基线：vitest 落地，纯函数回归用例锁死估算与解析层

- **动机**：仓库此前零自动化测试（无测试框架、无 CI），v0.18.0→v0.20.2 连续五个
  版本修复的都是同一族 bug——估算行数与实际渲染漂移导致的输入框跳位/吞行。本版
  先把纯函数层守起来；「估算==实际渲染」的渲染级断言作为后续 stretch goal
- **测试框架**：引入 `vitest` 作 devDependency（不进运行时依赖，遵守运行时依赖
  克制约定）。`pnpm test` 运行；`pnpm typecheck` 扩展为双 tsconfig——
  `tsconfig.json` 排除 `*.test.ts` 保持构建产物干净，新增 `tsconfig.test.json`
  让类型检查覆盖测试文件
- **用例范围**（6 个文件）：
  - `ui/estimate.ts`：`estimateItemHeight` 六类条目黄金值、审批/问题卡高度、
    `headTailPreview` 首尾截断、`truncateLine` CJK 宽度、`questionOptionRow` 标记
  - `path-drops.ts`：`tokenizePathList`（引号/转义/file:// URI/未闭合引号）、
    `expandPath`、`imageMediaTypeOf`、`parsePathChunk` 判定
  - `workspace-files.ts`：`scorePath` 前缀优于包含、子序列匹配、`fuzzyMatchPaths`
    排序与 limit
  - `ui/theme.ts`：`resolveTheme`/`isThemeSetting`、`hexLuminance`/`contrastRatio`
    色彩数学不变量
  - `approval-memory.ts`：`ApprovalMemory.key` 的语义键规则
  - `markdown.ts`：`fitWidths` 收缩数学（最宽优先/下限/塌缩）+ `renderMarkdownLines`
    空行折叠/列表/代码块/表格冒烟
- **配套 export**：`hexLuminance`、`contrastRatio`（`ui/theme.ts`）、`scorePath`
  （`workspace-files.ts`）、`fitWidths`（`markdown.ts`）由模块私有改为导出，
  纯增量无行为变化
- **影响面**：无任何运行时行为变化

## [0.20.2] - 2026-08-31

### 修复：状态栏尾字符被裁的真因 — ink 逐行重写的 EL 会擦掉贴边最后一格（0.20.1 诊断修正）

- **0.20.1 的 ambiguous 宽度理论被用户实测推翻**（Ghostty 下依旧 `hig`）：Ghostty
  对 ambiguous 字形本就按窄渲染，宽度并未超限。重新取证后定位真因，证据链完整：
  - **ink 侧**（源码核读）：增量渲染器每重写一行 = `cursorTo(0) + 内容 + \x1b[K + \n`；
  - **Ghostty 侧**（源码核读）：写入最后一列后光标停在 `cols-1` 并置位
    `pending_wrap`（延迟换行），而 `eraseLine(.right)` 从 `cursor.x` **含**开始擦除
    —— 于是「内容恰好填满到最后一列的行」每次被重写时，都会把自己刚写上去的最后
    一个字符擦掉；
  - **字节级实测**：状态栏行渲染宽度恰好 = 终端列数（贴死最后一列），首次全量绘制
    不带 EL（字符完整），之后每次增量重写都带 EL——`h` 写上即被擦掉，永远补不回
  - 状态栏右段被 flexGrow 间隔推到贴边，是全界面唯一右对齐元素，所以只有它稳定
    丢尾字符；同机制下全宽边框盒（输入框/菜单）被重写的行也会丢右边框（已实测确认）
- **修复**：动态区整体右缩 1 列（`marginRight={1}`）——活界面从此**永不写入终端
  最后一列**（ncurses 时代「不写最后一格」铁律），写完内容后光标正常停在合法列，
  EL 只会擦到本来就空的最后一列，陷阱不再成立。Static 区只写一次、无 EL，横幅与
  用户消息条保持全宽不变
- **宽度基准同步**（保证 v0.18.0 建立的「估算=渲染」逐行对齐不破）：活区宽度基准
  `liveColumns = 终端列数 − 1` 贯穿输入框高度纯函数、图片托盘、菜单面板截断、
  自由文本详情、模式行、排队消息行、审批/问题卡估算器；静态区估算器（工具卡、
  用户条、markdown）维持原列宽不动
- **0.20.1 的保守宽度口径保留**：对真正以双宽渲染 ambiguous 字形的终端（如 iTerm2
  的对应开关）仍是正确的防线，与本次修复叠加生效

## [0.20.1] - 2026-08-31

### 修复：状态栏右端最后一个字符被裁掉（`推理 high` 显示成 `hig`）

- **排查**：会话日志里请求头存的是 `"reasoningEffort":"high"`，store 与状态栏全程
  原样传递、无任何截断——数据完好，是**渲染裁切**：状态栏右侧用量段被 flexGrow
  间隔推到紧贴终端右边缘，行内 `●`/`·`/`↑`/`↓` 属于 East-Asian-Ambiguous 字形，
  string-width 记 1 列而「ambiguous 双宽」配置的终端（CJK 环境常见，如 iTerm2 的
  对应开关）实际画 2 列；预算公式还漏算了行首 spinner 字形 1 格。布局余量 5 格
  < 未记账的 6 格，真实绘制恰好超宽 1 格 → 最右侧的 `h` 被推出屏幕裁掉
- **修复**：状态栏宽度预算整体改用保守口径 `displayWidth`（本组件实际用到的
  ambiguous 字形按 2 列计），并把 spinner 字形计入预算——测量值从此 ≥ 任何终端的
  真实绘制宽度，放得下就完整显示（实测该行 71 格，76 列以上终端全量可见），
  放不下时按既有优先级先丢「推理」段、再丢用量段，任何终端都不会再裁尾
- **影响面**：仅状态栏自身的降级数学；全局 `truncateLine`/`textRows` 保持与 Ink
  折行一致的 string-width 口径不动（Ink 的行数记账以它为准，改动反而会破坏
  v0.18.0 建立的填充预算精确性）

## [0.20.0] - 2026-08-31

### 功能：斜杠菜单模糊匹配 — 子序列命中 + 匹配字符加粗 + ←→ 展开描述

- **动机**：命令过滤原先是纯前缀匹配，`/dk` 找不到 /doctor、`/cfg` 找不到 /config，
  必须记得完整命令名；调研里 Codex 的 popup（精确 > 前缀排序、命中字符加粗）与
  Gemini 的 ←→ 展开长描述是两个直接可抄的形态
- **模糊匹配**：前缀命中最优先（保持注册顺序），其余按**有序子序列**匹配打分——
  连续命中、更早命中、跨度更紧的排前面；`/dk`、`/cfg`、`/ss` 这类缩写都能直达。
  命中字符在菜单行中**加粗显示**，一眼可见为什么这条命令被匹配
- **←→ 展开描述**：命令菜单打开时按 ←/→ 在「40 列摘要」与「完整描述」间切换
  （仍截断到面板内宽，单行承诺不破，8 槽固定高度预算不受影响）；@ 文件菜单的
  ←→ 保留光标移动语义（会改变 @ 词本身），提示行只在命令菜单显示 `←→ 描述`

### 改进：并行工具运行行合并 + 轮末工具计数提示

- **并行合并**（Codex `• Ran N commands` 的对应物）：多个工具并行运行时，动态区
  原先每个调用占一行 `⚙ … 运行中…`，现合并为一行 `⚙ 并行运行 N 个工具…`，完成后
  仍逐个定稿为独立卡片；弹性填充预算同步改为「有运行中工具即 1 行」
- **轮末计数**：一轮 ≥2 次工具调用结束时，追加一行暗色提示 `· 本轮共 N 次工具调用`，
  给密集的调用序列一个视觉收束点（单工具轮保持安静）

## [0.19.0] - 2026-08-31

### 功能：问题卡箭头键化 — ↑↓ 高亮选择 + 数字键直选双通道（解除 9 选项上限）

- **动机**：除斜杠补全外的所有选项菜单（agent 提问、计划审批、/sessions、/model、
  /effort、/theme、/config）都复用 `QuestionView` 的「数字 1–9 + Enter」交互，被锁死
  在 9 个选项上限——/sessions 只显示最新 9 个会话、/theme 被迫 8 个一页分页。调研
  （`docs/ui-research-2026-08.md`）显示 Claude Code / Gemini / Codex 的选择器全部以
  箭头键为主通道
- **行为**：↑↓ 在选项间移动高亮（`❯` 反色标记，环绕滚动），**数字键保留**为「跳选」
  快捷（Gemini RadioButtonSelect 的双通道模式：数字直接选中窗口内对应项）；Enter
  确认高亮项（计划审批的光标初始停在「批准」上，裸 Enter 仍是批准）；Esc 跳过不变
- **滑动窗口**：卡片一次显示 9 个选项，更长的列表在窗口内滚动并显示 ▲/▼ 溢出标记
  ——/sessions 可列 100 个会话、/model 不再截断、**/theme 的 Ghostty 选择器去掉
  8 个一页的分页**（14 款单列直选）
- **实现**：高亮光标与窗口偏移存进 store 的 `ActiveQuestion`（而非视图局部状态），
  估算器与渲染共用同一份窗口状态，弹性填充预算逐帧精确

### 功能：工具卡片紧凑化 — 单行状态头 + 缩进预览，去边框盒（信息密度对齐 Codex）

- **动机**：原先每个完成的工具调用是一张圆角边框卡片（空行 + 双边框 + 头行 + 最多
  12 行输出 ≈ 17 行），一轮 N 个调用 N 张框；调研确认没有任何一家竞品默认画边框盒
  ——共识形态是「一行摘要 + 个位数行预览 + 显式展开」，颜色只标记状态不充当容器
- **行为**：卡片改为**单行状态头**（`✓/✗ 命令或标题 · 耗时[ · exit N]`，成功绿/失败红，
  长命令自然折行）+ **两格缩进的暗色输出预览**，无边框。预览预算：终端/generic 5 行、
  diff 8 行、搜索路径 3 行、web 来源 3 行（Ctrl+O 切换完整显示的语义不变）；
  输出截断改为**头尾保留**（首 4 行 + 末 1 行，Codex 的 output ellipsis 模式——
  结尾常承载报错/结果摘要）。一行 Bash 调用的默认占屏从 17 行降到 ~8 行
- **估算器重写**：`estimateToolCardHeight` 与新渲染逐行对齐（头部经 textRows 精确
  计行，预览行截断保单行），弹性填充预算不受布局变化影响

### 功能：思考折叠摘要行 — ✻ 思考：首行摘要 · 用时

- **动机**：思考内容原先完全隐藏（只有状态栏「已思考 N 字」的实时计数），回合结束后
  无任何痕迹；Pi / OpenCode 的默认形态是折叠一行「标题 + 时长」
- **行为**：每步推理结束时在回复前定稿一行暗色摘要 `· ✻ 思考：<首行非空行> · <耗时>`
  （耗时为该步首个 reasoning delta 到最后一个的实测窗口）；无首行时显示字数。
  回放的会话不含推理增量，转录中不带此行（与原行为一致）

### 改进：Ctrl+O 切换完整显示时补开最近一次调用的明细面板

- **动机**：Static 转录项定稿后永不重绘（Ink 架构约束），全局开关无法改变已定稿卡片
  的显示；但用户切 Ctrl+O 的典型诉求是「看刚才那条被截断的完整输出」
- **行为**：切到完整显示时，若最近一个定稿项是工具卡，则以面板形式补打其完整输出
  （按终端视口高度截断），紧随开关提示之后

## [0.18.0] - 2026-08-31

### 修复：输入框钉底根治 — 布局预算与绘制同一 commit 同步（弹性填充不再晚一帧）

- **动机**：输入框「有时往上跳、不回来」是长期痛感——CHANGELOG 里 0.5.1 / 0.6.x /
  0.7.0 / 0.13.0 / 0.13.4 反复修的是同一族问题。根因调研（详见
  `docs/ui-research-2026-08.md`）定位为结构性缺陷：输入框/菜单的高度在子组件的
  effect 里**事后上报**，而 Ink 在 React commit 的 mutation 阶段就已把帧画出——
  任何让输入框变高的提交（长行换行、多行粘贴、历史召回、补全菜单打开、图片托盘）
  都先画出一帧「超预算」的帧，终端物理滚动 N 行后，Ink 的增量渲染器光标模型与
  屏幕永久失配，输入框锚点整体上移且不可自愈
- **修复**：把编辑器状态（`ed`）与补全菜单状态从 `InputBox` 提升到 `App` 持有，
  输入框高度改为渲染期同步派生的纯函数（`computeInputHeight`），弹性填充预算在
  **同一个 commit** 内拿到新高度——超预算中间帧从机制上不再存在。菜单开合（含
  @ 文件菜单的异步加载完成帧）随之并入同一同步路径
- **附带**：resize 重建不再需要手工预估首帧输入框高度（`initialInputHeight` 移除，
  App 直接从恢复的草稿态派生）

### 修复：行数估算失配清扫 — 排队消息 / 运行中工具 / 菜单行 / 自由文本详情

- **排队/注入消息指示行**：原先渲染宽度可达 `列数+19`（折 2 行）而预算只计 1 行，
  运行中每次提交消息必失配一次；现整行按终端宽度截断，渲染与预算严格 1 行
- **运行中工具行**（`⚙ … 运行中…`）：长命令标题原先可换行溢出预算，现截断为 1 行
- **补全菜单行**：label + 描述按菜单面板内宽截断，长文件路径不再折行撑破 8 槽
  固定高度预算；菜单提示行同样截断
- **自由文本问题的 detail**：原先不截断渲染但按 1 行预算（必失配），现截断为 1 行
- **审批/问题卡**：选择行与提示行改为按实际字符串精确计行（窄终端下可换行，
  原先一律 +1 会低估）
- **`truncateLine` 换 string-width 口径**（UI 估算与 store 两处一致）：自研 charWidth
  对 emoji 记 1 列而终端实际占 2 列，截断结果可能超宽折行——宽度口径与 Ink 的
  string-width 对齐后，所有「截断保单行」的约定真正成立

### 新增：帧高自检（flicker detector，仿 Gemini CLI useFlickerDetector）

- **动机**：这类布局 bug 没有报警器就只能靠肉眼发现；Gemini CLI 的实践是根元素
  实测高度超过终端高度即记一次 Flicker 事件（「这是渲染 bug 该修的标志」）
- **行为**：每帧在 layout effect 里用 `measureElement` 量动态区根元素，超过终端
  行数时写 `/tmp/fx-debug.log`（`frame-overflow`，`FX_TUI_DEBUG` 开启时），
  便于回归定位

### 文档：TUI 交互调研报告

- 新增 `docs/ui-research-2026-08.md`：Codex / OpenCode / Pi / MiMo-Code（及
  Claude Code、Gemini CLI、Crush）在「钉底输入框 / 菜单 / 工具展示」三题上的
  架构对比、各自 issue 记录与本次实施对照

## [0.17.0] - 2026-08-29

### 功能：长任务完成通知 — 终端铃声 / macOS 系统通知（/config notify）

- **动机**：把长任务挂在 fx 里、人切去别的窗口是常态，回合结束毫无感知，
  只能时不时切回来看一眼；这是日用痛感最大的一项
- **触发口径**：`turn/start` → `turn/end` 计时，**超过 10 秒**的回合结束时提醒
  （短回合用户还在看屏幕，不打扰）；用户主动 Esc 中断的回合不提醒
- **三种模式**：`bell`（默认，写 BEL，终端的铃声/视觉提示设置决定表现形式）/
  `system`（osascript 弹 macOS 通知中心 + 提示音，`任务完成（耗时 N 秒）`，
  出错回合为「任务出错」）/ `off`；`/config notify <off|bell|system>` 直改或
  `/config` 交互选择，持久化到设置文件

### 功能：Ctrl+V 剪贴板粘贴 — 文本直插光标处，图片自动附加

- **动机**：图片输入原本只有拖拽一条路；截图（⌘⇧⌃⇧4 进剪贴板）是 mac 上
  最高频的图片来源，却要先进 Finder 再拖，纯多余一步
- **行为**：`Ctrl+V`（或 `Opt+V`，防部分终端拦截 Ctrl+V）读取系统剪贴板——
  有文本就按普通多行插入（粘贴通道同款，经编辑器的纯函数 splice，异步读取
  期间继续打字不丢字）；无文本则尝试位图：AppleScript 读剪贴板 PNG（部分应用
  只放 TIFF 时经 sips 转 PNG），经 dsh attachment 服务校验后进附件托盘，随下
  一条消息发送，命名 `clipboard-<时间戳>.png`；两者皆空才提示「剪贴板中没有
  文本或图片」。零新增依赖（pbpaste / osascript / sips 均为 macOS 自带）

### 功能：输入历史跨会话持久化

- **动机**：↑/↓ 翻阅的输入历史只存在内存里，重启即失；「上次那条命令怎么写
  的来着」是最常见的回访需求
- **行为**：提交过的消息落盘 `$DSH_HOME/fx-tui-input-history.json`（纯 JSON
  数组，上限 500 条、连续重复折叠），启动时载入；删除文件即清空。编辑器依旧
  按引用读活数组，浏览无额外渲染开销

### 功能：steering 三语义 — Enter 注入当前轮 / Tab 排队下一轮 / Alt+↑ 取回

- **动机**：原来 agent 运行中提交只有「排队到下一轮」一种语义；想纠正正在
 进行的方向（「不对，换个思路」）只能打断重来，或干等回合结束
- **Enter（运行中）＝注入**：`agent.steer()` 把消息送进最近一步的边界，模型
  在当前回合的下一步就看到；指示器显示 `🧭 已注入（下一步生效）`。空闲时
  Enter 语义不变（新回合）
- **Tab（运行中、无补全菜单时）＝排队**：保持原 `followup` 行为，`⏳ 已排队
  （下一轮生效）`；菜单打开时 Tab 仍是补全，互不冲突
- **Alt+↑＝取回**：最后一条还没被处理的消息（注入/排队均可）从 inbox 撤回
  （`inbox.remove`，durable 记录）并回填输入框修改重发；已被 driver 认领的
  消息不取回（正要进转录）。Esc 中断回合时排队/注入消息照旧随轮次丢弃并提示

### 功能：/effort 推理强度热切换 + 状态栏 TPS / 缓存命中率 / 推理等级

- **动机**：deepseek 系模型按档位（off/high/max…）分推理强度，fx-tui 一直
  没有入口；输出多快、缓存命中多少也全靠猜
- **/effort**：无参弹出档位选择器（来自 `ctx.llm.resolveModelInfo` 的
  adapter 档位表，标注当前/默认）；`/effort <id|名>` 直改；`/effort status`
  看全表。切换与 /model 同路（`ModelSelection.reasoningEffort`，下一步请求
  生效）并持久化为启动默认
- **状态栏增强**：用量段新增**缓存命中率**（缓存读 / 全部输入 token）与
  **输出速度 tok/s**（首个文本 delta → 消息装配的真实测量窗，短窗不显示避免
  荒谬值）；新增**推理等级**段（取自真实请求头 `request/header`）；右侧分段
  按优先级降级（先丢推理等级、再丢用量），上下文水位永远保留
- **上下文水位变色**：≥80% 琥珀、≥95% 红，越过阈值一次性警告（80% 提示
  /compact、95% 提示开自动压缩），压缩后水位回落自动重新武装

### 功能：/btw 侧问 — 复用上下文的单轮提问，不打断主任务

- **动机**：主回合跑着长任务时想顺口问一句「这个报错什么意思」，要么打断、
  要么开新会话重新贴上下文
- **行为**：`/btw <问题>` 直接经 `ctx.llm.stream()` 发一次**无工具单轮调用**
  （消息列表 = 当前模型可见面 + 侧问），答案以面板呈现（Markdown 渲染）；
  **不写入会话历史、不计 token 统计、不打断运行中的回合**；连续侧问后者取代
  前者（AbortController 中止旧流）

### 功能：/rename 会话重命名 + /sessions 标题展示与关键词过滤

- **/rename**：空参显示当前标题；`/rename <新标题>` 经 `ctx.sessionTitle.
  rename` 写 `session/title` 事件（user 来源，**钉住标题**：自动生成停止），
  /sessions 列表与 /status 面板随即按标题展示
- **/sessions**：列表批量折叠标题（`readTitleSnapshots`，一次观测），标签
  `标题 · 时间 · 目录 · 运行中`；`/sessions <关键词>` 按标题/目录/会话 id
  过滤后再弹选择器；同分钟同目录的重复标签自动加 `#N` 后缀保证回查唯一

### 功能：/context 上下文明细 + /doctor 环境自检

- **/context**：水位（token-meter 的 provider 锚定值）+ 组成估算（系统提示/
  工具定义/对话消息三段，按最新 `request/header` 包络启发式估算，标注仅供
  看占比）+ 最近一次请求的 provider 用量报告（输入/输出/缓存读/缓存写/推理）
- **/doctor**：Node 版本、平台、dsh 内核版本、模型路由与能力（上下文窗口、
  推理档位数）、API 凭证（环境变量或 `~/.dsh/.credentials.yaml`）、设置与
  输入历史文件、终端 TTY/尺寸/TERM、工作目录——逐项 ✓/✗/·，失败项直指修法

### 功能：可选自动压缩（/config autocompact on|off，默认关闭）

- **动机**：95% 水位警告了但人不在，长会话照样撞墙；压缩改写历史，必须用户
  明示开启而不是默认行事
- **行为**：开启后，agent 进入 idle 且水位 ≥85% 时经 `ctx.compaction.
  compactIfNeeded(agent, 'pressure')` 自动压缩（dsh 内核的压力策略决定压哪
  段），完成即刷新水位并通知；一个高压周期只尝试一次，水位回落自动重置；
  失败/无可压区间都降级为提示，绝不影响回合循环

### 修复：审批请求撤销不再悬挂 promise

- **动机**：上游 abort 审批请求时 `cancelApproval()` 只清了 resolver 不
  resolve，`approval/request` waterfall 的 await 永远挂着（review-report 遗留 P0）
- **行为**：撤销现在按 **fail-closed 拒绝** resolve（与 Esc=拒绝一致），并
  留下「审批请求已撤销，按拒绝处理」提示

### 影响面

- 新增模块：`src/history.ts`（输入历史）、`src/clipboard.ts`（pbpaste/
  osascript/sips 封装）、`src/notify.ts`（通知三模式）；`store.ts` 事件归约
  扩展 `request/header`（推理等级）与流式计时窗；`ui/Input.tsx` 键位新增
  Ctrl+V / Tab 排队 / Alt+↑；`ui/StatusBar.tsx` 右侧分段着色改造
- 设置文件新增 `notify` / `autoCompact` 两键（缺省不落盘，老文件兼容）；
  类型依赖新增 `@deepseek-ai/dsh-compaction`、`@deepseek-ai/dsh-session-title`
  （0.1.1-rc.2，devDependencies，仅取类型声明，运行时服务由 dsh-base 提供，
  **零新增运行时依赖**）
- 全部新功能经真实 dsh 环境端到端验证（pty 驱动：/doctor /context /btw
  /effort /rename /sessions 实跑、真实回合的 steer 注入与撤销丢弃、缓存命中
  率与 tok/s 展示、剪贴板 PNG 读取链路、通知三模式）。版本号升至 0.17.0

## [0.16.1] - 2026-08-29

### 修复：技能菜单呈现打磨 — 简介截断 + 显式双分组标题

- **动机**：0.16.0 把技能简介原样铺进菜单，技能 frontmatter 的简介动辄一两百
  字（如 fx-ppt 的英文长句、add-to-armory 的整段中文触发说明），菜单行又长
  又嘈杂，极端时还会换行撑破 8 槽固定高度预算；同时命令组没有标题，两组的
  边界只靠位置暗示，不够一目了然
- **简介截断**：命令与技能的菜单简介统一按显示宽度截断到 40 列（CJK 宽度
  感知，复用 `ui/estimate.ts` 的 `truncateLine`，零新增代码路径），超长以
  `…` 收尾，菜单行恒为单行
- **显式分组**：菜单顶部新增「命令」分组标题，技能组保留「技能」标题，两
  组标题同为 dim 色分隔样式、不可选中、↑↓ 自动跳过；某组被筛选清空时对
  应标题随组一同隐藏。版本号升至 0.16.1

## [0.16.0] - 2026-08-29

### 功能：`/` 菜单并入技能分组 + `/技能名` 手势直通

- **动机**：dsh 上游早已具备完整技能体系（`dsh-skill` 注册表 + 文件系统提供方
  扫描项目 `.dsh/skills`、`.agents/skills` 与用户 `~/.dsh/skills`、
  `~/.agents/skills`；`dsh-tool-skill` 向模型注入 `<available_skills>` 目录并
  注册 `skill` 工具），但 fx-tui 的 `/` 菜单只列命令，技能对用户完全不可见；
  且提交分流把「单行、以 `/` 开头」的输入全部拦截进命令分发，未知命令直接
  报错——上游原生的 `/技能名` 调用手势（`agent/pre-step` 钩子识别消息中的
  `/name` token 并自动注入技能全文）在行首位置永远无法到达 agent
- **菜单双分组**：`/` 弹出的补全菜单在命令之后新增**技能分组**（dim 色分组
  头一行，不可选中），数据来自 `ctx.skills.list()` 过滤 `userInvocable`
  （仅列手势真正能触发的技能）；命令与技能随输入**实时筛选**（统一前缀匹
  配）；↑↓ 导航自动跳过分组头，滑窗按「行」计（分组头占一可见槽，8 槽高度
  预算不变，输入框仍钉底）；Tab 补全高亮项，Enter 执行命令或插入技能；
  同名时命令优先（菜单去重与分发顺序两处一致）。技能目录启动时预取缓存、
  订阅 `skills/change` 事件失效重取，切换会话（cwd 可能变化）后重取
- **手势直通**：菜单选中技能只是往输入框插入上游原生的 `/技能名 ` 文本
  （fx-tui 不发明任何新协议，注入、回放、transcript 元数据全由上游维护），
  用户补上任务描述回车即触发；`runCommand` 对两个命令注册表都未命中的名字
  增加兜底——`ctx.skills.get()` 查到 user-invocable 技能时整行按普通消息发
  送（手势生效），否则才报「未知命令」（提示语同步提及技能）。消息中部直接
  写 `/技能名` 的上游原生路径不受影响
- **影响面**：`ui/Input.tsx` 菜单从纯条目列表泛化为「条目 + 分组头」行模型
  （`@` 文件菜单同构复用，行为不变）；`index.ts` 注入 `skills` 服务、新增
  技能缓存与刷新；`/help`、启动帮助、README 同步登记。类型依赖新增
  `@deepseek-ai/dsh-skill`（0.1.1-rc.2，devDependencies，仅取 `ctx.skills`
  的类型声明，运行时服务由 dsh-base 提供，**零新增运行时依赖**）；运行时经
  探针插件在真实 dsh 环境验证（本机 79 个技能全部 user-invocable，
  `get('fx-ppt')` 可加载完整定义）。版本号升至 0.16.0

## [0.15.0] - 2026-08-29

### 功能：收录 14 款 Ghostty 热门主题（深色 10 + 浅色 4）

- **动机**：0.14.0 的双主题（浅色/深色）解决了黑底可读性，但两套配色仍显单调；
  用户希望能像换终端主题一样换 fx-tui 的观感
- **主题选定口径**：Ghostty 官方并不发布主题热度数据（无遥测、`+list-themes`
  纯字母序，无 popular 标注——调研核实过源码与本机 1.3.1 二进制），因此按
  **公开 dotfiles 使用量**统计（GitHub 代码搜索 ghostty 配置中的 `theme =`
  赋值频次，2026-08）：取最热门的十个主题家族、每家族收录其最热门变体（全
  部深色）——Catppuccin Mocha、TokyoNight Night、Gruvbox Dark、Rose Pine
  Moon、Dracula、Kanagawa Wave、Nord、Ayu、Everforest Dark Hard、Solarized
  Dark Higher Contrast；另按同一口径补录使用量最高的四款浅色变体（Catppuccin
  Latte 41 处、Gruvbox Light 14、Rose Pine Dawn 11、TokyoNight Day 8），让
  浅底终端用户同样有得选，主题菜单中的 深/浅 标注即来自各主题自身背景色的
  亮度判定；色值自本机 Ghostty 1.3.1 官方主题文件**程序化提取、原样移植**
  （上游源为 mbadolato/iTerm2-Color-Schemes，`ui/ghostty-themes.ts` 纯数据
  文件）
- **语义 token 派生（自适应槽位）**：每款主题的 16 色 ANSI 调色板按语义槽位
  映射（cyan→accent、yellow→warning、green→success、red→danger、blue→info、
  magenta→approval），normal/bright 变体按「与主题自身背景对比度更高者」逐槽
  选择——浅色主题的槽位约定差异极大（Gruvbox Light 把更深的颜色放在 bright
  槽、Rose Pine 两槽同色、TokyoNight Day 完全相同），固定规则无法通吃；柔和
  调校的主题（如 Rose Pine Dawn 的金色）若对比度不足 2.5:1，向主题前景色渐
  进混合兜底（保色相、复可读），muted 槽因叠加 dimColor 用更强的 3.5:1 下限；
  用户消息条为「主题背景向 accent 混色（深色 32% / 浅色 28%）」的着色底 +
  主题前景文字；代码块高亮、diff 着色、markdown 样式全部由同一套 token 派生，
  零新增依赖。14 款主题的全部语义色经脚本断言：深色款 ≥ 3:1，浅色款 ≥ 2.5:1
  （柔和系主题的忠实移植限度，与内置浅色主题的 ANSI yellow 实际水平一致）
- **`/theme` 升级**：交互选择改为两级——基础档（自动检测 / 浅色 / 深色 /
  Ghostty 精选 14 款）+ Ghostty 主题分页列表（数字键问题面板上限 9 项，故
  8 个/页 + 下一页/上一页/返回导航，14 款分两页：第 1 页深色 8 款、第 2 页
  深色 2 款 + 浅色 4 款）；直给形式支持主题 id、显示名（如
  `/theme Catppuccin Mocha`，空格连字符等价）与原有中文别名，未知主题名弹
  出全量可用列表面板；`/status` 主题行在保存档与当前显示一致时不再重复
  显示两遍；选择持久化到设置文件（`theme` 字段存主题 id），非法值回退 auto，
  切换即全量重挂重绘。版本号升至 0.15.0

## [0.14.0] - 2026-08-28

### 功能：双主题配色 + 终端背景色自动检测

- **动机**：此前整套 UI 按作者的浅色终端调校，使用黑/深色背景终端的用户反馈
  部分文字看不清——根因是界面大量使用具名 ANSI 色（`gray`/`blue` 等），深色
  终端主题会把它们重映射成低亮度色调，落在黑底上近乎隐形
- **双主题**：新增语义化调色板层（accent/warning/success/danger/info/approval/
  muted/userBar 等 token，`ui/theme.ts`），全仓 40+ 处内联颜色字面量全部收编；
  浅色主题保持既有颜色**原样**（老用户视觉零变化），深色主题全部改用 hex 色
  值——绕过终端对 ANSI 色的重映射，可读性由配色自身保证（非 truecolor 终端
  由 chalk 自动降级），色调仍锚定品牌 ~181° 青色家族；用户消息条反转为深青底
  浅字，代码块高亮（cli-highlight）同步换用为黑底调校的 hex 主题（默认主题的
  蓝色关键字/绿色注释在黑底上同样看不清）
- **自动检测（默认 auto 档）**：启动时（Ink 接管终端前）发 OSC 11 背景色查
  询并短暂等待响应（120ms 超时，期间 stdin 临时进 raw mode、结束后原样恢
  复），按 sRGB 相对亮度判定深浅；不响应时回退 `COLORFGBG` 环境变量约定，仍
  测不出按浅色。iTerm2 / Terminal.app / kitty / Ghostty / VS Code / Warp /
  Windows Terminal 等主流终端均支持 OSC 11；代价是不响应的终端启动多等约
  120ms，探测全程 try/catch 不影响启动
- **`/theme` 命令**：交互选择（自动检测 / 浅色 / 深色）或直给（`/theme
  auto|light|dark`，中文别名同义）；选择持久化到 `$DSH_HOME/fx-tui-settings.json`
  （auto 不落盘，与 autoUpdate 同约定）并立即生效——复用 resize 全量重建机
  制（卸载重挂 + 清屏 + 全量重放转录），整个界面统一成新配色（代价同 resize：
  scrollback 清空；重挂前清空草稿捕获，避免把已提交的命令文本回填进新输入框）。
  `/status` 增加主题行，`/help` 与 README 命令表同步登记。版本号升至 0.14.0

### 文档：声明平台支持现状，存档 Windows 适配调研

- **README 平台说明**：安装节顶部新增显著说明——fx-tui 目前仅在 macOS 上
  开发和验证，暂不支持 Windows，Windows 用户可先用 dsh 上游自带的 Web UI
  （`dsh web`）
- **Windows 适配调研存档**：新增 `docs/windows-support-notes.md`，完整记录
  2026-08-28 那轮调研——dsh 上游对 Windows 的官方支持证据（TUI 支持、pwsh
  默认执行器、原生 CI、ConPTY 二进制）、fx-tui 侧逐文件断点清单（已兼容/
  会坏/降级三类）、适配方案骨架（fx.cmd 启动器、update.ts 跨平台执行器、
  路径处理修复等，零新增依赖）与维护负担分析；当前决定为暂缓适配，未来
  重启时以该文档为准，避免重复调研

## [0.13.4] - 2026-08-28

### 修复：终端窗口 resize 后排版错乱，且调回原尺寸也不可逆

- **问题**：在会话中多次拖拽调整终端窗口大小后，界面排版错乱（残行、重影、
  输入框与历史错位、同一条消息反复出现多份），且把窗口调回最初尺寸也无法
  恢复，只能退出重进
- **根因（两层）**：
  - 定稿转录经 Ink 的 `<Static>` 一次性写入终端后，Ink 不再管理这些行；终端
    resize 时按新宽度重新折行（reflow），而 Ink 对自身动态区域的行数记账仍
    停留在旧布局——此后每次重绘都按错误偏移擦写屏幕，且不可自愈
  - 修复试探过程中确认：任何「只重放部分转录、保留 scrollback 旧副本」的方
    案都存在不可对齐的接缝——旧 reflow 副本与新渲染副本无法逐行对应，重放
    不足则头部重复、过头则中部内容再滚入重复（实测同一消息出现三份）
- **修复（resize 全量重建，最终形态）**：监听 `stdout` 的 `resize` 事件，防
  抖 300ms（拖拽窗口的事件风暴只认最终尺寸）后卸载 Ink 实例，`ESC[2J` 清视
  口 + `ESC[3J` 清 scrollback + 光标归位，再以新宽度重新挂载、**全量重放转
  录**：scrollback 与视口的内容来自同一次输出流，超出视口的头部自然滚入
  scrollback 成为唯一副本——零重复、零缺失、无缝衔接（pty 实验六种宽高组
  合断言通过）。代价是 resize 后启动前的 shell 回滚历史被清空，以换取转录
  的绝对完整
- **草稿不丢**：输入框编辑器状态（文本 + 光标）经模块级 `draftCapture` 每次
  渲染时镜像保存，重挂后以 `restore` 原样恢复，并精确传入恢复草稿 + 图片托
  盘的首帧输入框高度、首帧 filler 额外收缩 2 行，保证首帧不会高过视口把
  banner 顶出屏幕；斜杠菜单等由编辑器内容推导的状态随之自动重建，审批/提
  问/流式回复等状态存于 store，不受重挂影响
- **兜底**：无实际尺寸变化的 resize 事件直接忽略；重建进行中新到的 resize
  事件排队防抖补跑，不并发重挂；退出与插件卸载时摘除监听并清掉挂起的重建
  定时器。行高估算器自 App 迁出为 `ui/estimate.ts` 共用。版本号升至 0.13.4

## [0.13.3] - 2026-08-28

### 修复：状态栏用量排版——`·` 只保留组间一层

- **问题**：状态栏右侧「上下文 2%·21.5k/1.0M · ↑198 ↓724 · 缓存↑20.6k」里
  `·` 同时承担两个层级——组内连接（`2%·21.5k`，无空格）与组间分隔（` · `，
  带空格），仅靠空格有无区分层级过于隐晦，整行拥挤、事实边界模糊。另有
  `缓存↑` 的箭头与前段 ↑/↓（输入/输出流量）语义冲突，易误读成「缓存上涨
  了 20.6」；且 CJK 标签后空格不一致（「上下文 2%」有空格、「缓存↑」没有）
- **修复**：
  - 上下文段改用半角括号把百分比与绝对值绑成同一事实：`上下文 2%
    (21.5k/1.0M)`——比例是扫一眼的结论、绝对值是注脚；同时把 ` · ` 完整
    让给组间分隔，整行恢复「三个事实、两颗点」的节奏，与底部帮助行、欢迎
    横幅的既有分隔习惯一致
  - 缓存段去箭头、补空格：`缓存↑20.6k` → `缓存 20.6k`
- **效果**：状态栏右侧变为 `上下文 2% (21.5k/1.0M) · ↑198 ↓724 · 缓存
  20.6k`，宽度仅 +2 列，窄终端的降级逻辑不变。版本号升至 0.13.3

## [0.13.2] - 2026-08-28

### 修复：连续工具调用卡片间距过大——空 assistant 项不再进转录

- **问题**：连续多个工具调用的绿色卡片之间、以及用户消息条与第一张卡片
  之间，垂直间距 uniform 地偏大（实测恰好 2 行空白），视觉上像卡片各带了一
  圈 margin
- **根因**：模型连续调工具不说话时，携带 `tool_use` 块的那条 assistant 消息
  没有任何 text 块，而 `assistant/message` 事件处理是无条件入列转录——产出
  `text: ''` 的空 assistant 项；空项渲染时 = 项前置空行（LeadGap）1 行 +
  markdown 渲染器对空输入兜底返回的 `' '` 行 1 行，恰好 2 行空白。工具卡片
  本身零间距，全部可见间隙都来自这些「幽灵空项」。纯视觉问题：高度估算器
  对空项同样按 2 行计，与实际渲染一致，不引发帧错位
- **修复（两层）**：
  - store 侧：`assistant/message` 的文本为空且未中断时不再入列转录项（空消
    息没有任何可见内容）；中断的空消息保留推送——它承载「回复被中断」提示
  - 视图侧：工具卡片补上与其他项统一的 1 行前置空白（LeadGap）——否则去掉
    幽灵空项后相邻卡片会边框贴边框；同步高度估算器 tool 分支 +1，估算与实
    际逐行一致
- **效果**：所有转录项之间统一为设计注释所述的 1 行空白节奏；对截图逐像素
  回验：行距 34px 下原间距为 3 个行距（2 行空白），修复后为 2 个行距（1 行
  空白）。版本号升至 0.13.2

## [0.13.1] - 2026-08-28

### 修复：表格列宽自适应，长内容截断带省略号

- **问题**：markdown 表格渲染器给每列设了 24 字符的硬上限（`Math.min(w, 24)`），
  超宽单元格直接 `slice(0, 24)` 切掉且不加任何省略号——含长路径、长包名、长
  URL 的表格每一行看起来都像「没显示完」，且无从分辨是内容本身就到那里还是
  被渲染器截断
- **修复（列宽自适应）**：列宽改为先按单元格内容算自然宽度，表格总宽（含
  ` │ ` 分隔）不超过当前换行宽度时完整展示、不做任何截断；只有真正超预算时
  才从最宽的列开始逐列压缩，短列优先保全，压到下限 4 列仍放不下才继续收缩
  到 1 列的极端兜底。常见宽度下（终端 ≥ 80 列）两三列表格基本都能完整显示
- **修复（CJK 对齐）**：列宽此前用 `.length`（UTF-16 码元数）计算，全角中文
  按 1 列计而实际显示占 2 列，中文表格的 `─┼─` 分隔线与单元格内容普遍错位；
  现改用 string-width 显示宽度统一计算列宽、截断与补齐，截断按码点逐字符累
  加宽度，不会把宽字符劈成半个。顺带把 `<br>` 产生的单元格内换行折叠为空格
  ，保证单元格严格占据一行（渲染器「一行一串」契约不被表格打破）
- **截断带省略号**：仍需截断的单元格以 `…` 结尾并补齐到列宽，被截一眼可见
  ，后续列的竖线分隔依然对齐。已用多宽度（100/60/40/26 列）实测：放得下时
  全内容展示、放不下时省略号截断，各宽度下每行显示宽度严格一致。版本号升
  至 0.13.1

## [0.13.0] - 2026-08-27

### 修复：补全菜单可滚动，命令不再被固定 8 条截断

- **问题**：输入 `/` 弹出的命令菜单从生成那一刻起就被截断到最多 8 条
  （`.slice(0, MENU_SIZE)`），而上下方向键只在被截断后的数组里移动高亮、
  没有任何滚动机制——内置命令已增至 10 个，加上 dsh 注册表注入的
  `/compact`、`/goal`、`/feedback` 等，排在后面的 `/update`、`/exit` 与全部
  dsh 命令永远不可见，只能靠手动敲前缀缩小匹配范围才够得着
- **修复（滑动窗口）**：菜单 state 改为保留完整过滤列表，渲染时只画一个
  8 行高的滑动窗口；上下键遍历完整列表，高亮走到窗口上下边缘时窗口随之
  平移，窗口内的移动则保持窗格纹丝不动。菜单 pane 高度依旧固定（不足 8 条
  空行补齐），输入框贴底的布局预算不受影响
- **修复（执行命令后输入框不贴底）**：在 slash 菜单里 Enter 执行 `/status`
  等命令后，输入框不再固定在终端底部，而是悬在屏中、底部留下一段空白。
  根因：命令入列面板的一帧里菜单尚未关闭，弹性填充的预算同一帧内同时计入
  面板（27 行）与菜单（17 行），帧总高 52 行超出 50 行视口——终端发生物理
  滚动后，ink 的增量渲染器不感知滚动、光标模型与屏幕永久失配，活跃帧锚点
  整体上移。修复为 Enter 执行命令时与 store 更新同批同步关闭菜单，帧高恰好
  回到视口高度，不再触发滚动。已用 pty 驱动真实 fx 对比修复前后帧序列验证
- **修复（行重叠的根因）**：菜单打开后用方向键滚动时，某一行的前半段和另
  一行的后半段叠在同一物理行上（如 `/export…Markdown` 后面跟着上一条的
  尾巴），高亮看似把一行拆成了两半。根因在 ink 7.1.1 的 yoga 集成：带
  `height` 约束的列式 Box 首次布局时文本节点被错误测高、相邻兄弟拿到相同
  纵坐标，后写者覆盖前者的头部、尾部残留；旧版菜单里任何按键都会改文本、
  触发重测量而自愈，加入滚动后纯高亮移动不再触发重排，错误布局得以稳定
  可见。修复为去掉菜单 Box 冗余的 `height` 属性——槽位本就固定渲染 8 行 +
  空行补齐 + 提示行，内容高度天然恒定。已用 pty 驱动真实 fx 逐帧回归：
  20 帧零重叠行，高亮计数与按键序列完全一致
- **位置提示**：列表超过 8 条时，菜单底部的操作提示行前缀「第 x/y 项」
  （不新增行数），滚动时随时可见当前进度
- **`@` 文件菜单同步受益**：模糊匹配的保留上限从 8 条放宽到 60 条，与命令
  菜单共享同一套滚动窗口；排序不变，原本的前 8 条仍是前 8 条。版本号升至
  0.13.0

## [0.12.0] - 2026-08-27

### 用户消息通栏色条 + 全文段间距统一

- **用户消息改为通栏背景条**：此前用户输入仅为 `❯ 消息` 的青色文字，与周围
  回复内容视觉区分弱。现改为 Claude Code 式整行渲染——用户消息的每一行都铺满
  整个终端宽度的淡青背景条（`#bdeef2`，深色粗体文字），色相刻意锁定欢迎横幅
  logo/边框的实际渲染色（采样实测约 181° 的 teal 青），新元素向既有品牌色靠拢
  而非另起一族；长行在终端边缘硬折行、折行后的每一行同样补满背景，绝不出现
  「第二行裸奔」；多行消息、空行同样整条铺色。刻意用十六进制固定色而非命名
  ANSI 色：终端主题会重映射命名色——浅色主题下 `gray` 背景被映射到近白、
  色条直接隐形（首版实测即栽在这里）；固定色值任何主题下都稳定可见，非真彩色
  终端由 chalk 自动降级到最近 256 色
- **段间距根因**：旧版 markdown 渲染器给每种块（段落/标题/列表/代码……）尾部
  都固定补一个空行，而 marked 词法器还会把块与块之间的空行额外产出为独立的
  space 块、每个再补一行——一个回复内部因此普遍出现两个连续空行的大豁口；
  反观用户输入与回复开头之间却零间距，节奏严重不均
- **修复（两层）**：
  - `src/markdown.ts` 新增 `pushBlank` 折叠逻辑——任意两块之间最多保留一个
    空行，回复开头不再产出前置空行，块内原有缩进/边框排版不变
  - `App.tsx` 给每条用户消息、每条助手回复（含流式中的实时预览）统一在顶部
    加一行间隔——全篇从此遵循同一节奏：「任何内容之间恰好一个空行」，消息
    边界与段落边界观感一致
- **布局预算同步**：弹性填充行的高度估算精确跟随新渲染行数（通栏行包裹 +
  补齐后的行数、前后置空行均计入），首屏定位与输入框贴底行为不受影响；
  版本号升至 0.12.0

## [0.11.1] - 2026-08-27

### 修复：/update 安装阶段 shell 语法错误

- **问题**：首次有用户在干净工作区真正跑完 fetch/快进合并后，升级在第 5 阶段崩掉：
  `(… && exec pnpm || …) install` 被 POSIX shell 判为非法——子 shell 复合命令
  `( )` 之后不允许直接跟裸参数，属纯语法错误，与用户环境无关。此前的冒烟验证
  因脏工作区拦截与「已是最新」短路都未触及该字符串，漏测到线上
- **修复**：改为 if 列表结构 `if command -v pnpm …; then pnpm <args>; else
  corepack pnpm <args>; fi`，两个分支均实测：参数透传正确、corepack 兜底可达；
  版本号升至 0.11.1
- **受影响用户的恢复路径**：他们的仓库已快进到出错前的最新提交、但依赖与构建产物
  未更新——拉取本修复后直接再跑一次 `/update` 即可；或立即手动执行
  `cd ~/fx-tui && pnpm install && pnpm run build`（之后也能正常用 /update）

## [0.11.0] - 2026-08-27

### 后台自动更新

- **启动即静默保活**：默认开启——fx 启动约两分钟后在后台执行一次「检查 → 拉取 →
  安装依赖 → 重建」，全程复用手动 `/update` 的流水线与防呆；成功后仅弹一条
  `✅ 已在后台更新到 vX，重启 fx 生效`，不打断当前会话，下次启动自然加载新版
- **联网克制**：每 24 小时最多真正联网检查一次（时间戳记于
  `$DSH_HOME/fx-tui-auto-update.json`），重启再频繁也不会骚扰网络
- **并发安全**：跨 fx 实例靠 `$DSH_HOME/fx-tui-auto-update.lock` 互斥（超过
  30 分钟的残留锁视为死进程遗物自动接管）；自动通道与手动 `/update` 共享同一
  忙碌标志，两条路径绝不交错执行
- **彻底静默的边界**：非 git 克隆安装、开关关闭、频控命中等情况全部无声跳过；
  唯一会出声的是更新未执行的失败场景——合并为一条通知并附 `/update` 提示，
  绝不重试轰炸；触发延迟使用 unref 计时器，永不拖慢进程退出
- **可一键关闭**：`/config autoupdate on|off` 直接改持久化设置；
  `/config` 交互面板新增第二个选择器；设置文件向后兼容（新字段缺省即默认开启，
  删除文件仍是一键重置）
- **新模块 `src/auto-update.ts`**：纯编排层（频控 + 锁 + 结果映射），状态以
  枚举返回便于测试；`performSelfUpdate` 结果新增 `versionAfter` 字段供提示文案取版本号

## [0.10.1] - 2026-08-27

### 修复：重复启动不再叠印残影

- **问题**：在终端里退出 fx-tui 后，若不手动执行一次 `clear` 就直接再次 `fx` 进入，
  新界面会叠印在上一场会话的残留画面上——死亡输入框、半截命令菜单、状态栏与新
  banner/输入框交叠错位，不得不每次先敲一遍 `clear`
- **根因（两处叠加）**：本应用为 Claude Code 式 inline TUI，不用备用屏幕缓冲区。
  退出时 Ink 的 unmount 对最后一帧零擦除，遗留画面原样留在主缓冲区；再次启动时
  挂载前仅有 `homeCursor()` 的 `\x1b[H` 光标归位，Ink 只重绘自己管理的区域、
  看不见主缓冲区的陈旧内容，新帧便直接盖在残骸之上
- **修复**：冷启动挂载前新增 `wipeViewport()`——向终端写入 `ESC[2J` 清空可视区。
  刻意不使用 `ESC[3J`（清 scrollback）：向上滚动仍能看到启动前的 shell 历史与更早
  的会话内容，维持 homeCursor 启动屏「滚轮回看 shell 历史」的既有设计契约；效果与
  手动 `clear` 等价但由程序自动完成。`/edit` 外部编辑器重挂载路径有自己的
  `bottomFlush()` 收尾逻辑，不受影响
- **验证**：单 PTY 实录「clear → fx → /exit → fx」完整字节流并以终端模拟器回放：
  修复前二次启动画面残留完整命令菜单与旧输入框，修复后视口干净、帧铺满无叠影；
  全程仅由本程序发出 `ESC[2J`、从未发出 `ESC[3J`
- 版本号升至 0.10.1

## [0.10.0] - 2026-08-27

### 自我升级：/update

- **新命令 `/update`**：在 TUI 内对当前安装目录执行完整升级流水线——
  `git fetch origin <分支>` → 比对远端 → `git pull --ff-only` → `pnpm install`
  （frozen 失败自动降级常规安装）→ `pnpm run build`，各阶段带进度通知与
  wall-clock 超时（超时强杀整棵进程树）。适用于 git 克隆安装（docs/install.md 流程）
- **安全防呆**：脏工作区（已跟踪文件的未提交改动）默认拒绝升级并列出文件，
  `/update --force` 显式放行；detached HEAD 与异常字符分支名直接拒执行；
  只允许 fast-forward，绝不制造本地 merge 提交；网络失败 / 合并冲突 / 构建报错
  均展示原始错误尾部并附具体修复命令
- **升级前先查远端**：本地与 `origin/<分支>` 一致时直接返回「已是最新」，
  不白跑 install/build
- **升级完成面板**：报告应用提交数与新版本号（通过 cache-bust 动态 import 新构建
  的 `lib/index.js` 读取 `FX_TUI_VERSION`，未升版本号时如实说明），并明确提示
  运行中的进程仍是旧代码、需退出重开 fx 才生效
- **新模块 `src/update.ts`**：shell 执行器（detached 进程组 + 树级 SIGKILL +
  输出尾部裁剪）、pnpm/corepack 双通道解析（login shell 保证 GUI 启动时的 PATH）、
  纯逻辑无 UI 依赖，由 index.ts 映射为通知与面板
- 其余安装形态一律拒自动升级并给出对应手动指引：node_modules 目录提示走包管理器
  （npm 分发启用后为 `npm i -g fx-tui`），缺 `.git` 目录指向 docs/install.md；
  `/help`、命令菜单、README 同步收录 `/update`；版本号升至 0.10.0
- **新增 `AGENTS.md` 长期协作约定**：为任意编码 agent 固化仓库工作流规则——每次
  提交/推送必同步 CHANGELOG、提交前 typecheck+build 必须通过、FX_TUI_VERSION 版本纪律、
  禁止裸 add 带入工作区杂物；文件随 git 分发，保证跨会话、跨设备、跨工具持久生效

## [0.9.0] - 2026-08-27

### 图片拖拽直入与附件托盘

- **拖图即附加**：从 Finder 把图片直接拖进终端窗口即自动附加到待发送队列，多选拖拽
  也支持——不再需要手敲 `/image <路径>`。原理：终端把「文件拖入」实现为在光标处
  粘贴该文件的路径文本（含空格时加引号或 `\ ` 转义），输入框据此拦截。仅当编辑器
  为空（或恰好是裸 `/image`）且整段内容解析为真实存在的图片文件时才自动附加；
  编辑器已有文字或非图片文件维持原有文本插入，绝不静默修改草稿。拦截覆盖两个输入
  通道：bracketed paste 与多字符块（兼容未开启括号粘贴的终端）
- **新模块 `src/path-drops.ts`**：shell 风格分词器（单/双引号、反斜杠转义、未闭合
  引号容错）+ 路径规范化（`~` / `~` 展开、相对路径锚定 cwd、`file://` URI 百分号解码）
  + 扩展名→media type 映射；纯函数实现，与 UI 解耦
- **`/image` 增强**：参数走同一分词器，支持 `'a b.png'`、`a\ b.png`、`~/x.png`、
  `file:///…` 等形态且**可一次附加多个**；某个文件读取失败只跳过该张、不中断批次
- **附件托盘可视化**：附加后品红计数行下新增暗色明细行，逐张显示
  `文件名（宽×高）`；高度估算与渲染共用同一文案生成函数，弹性填充区尺寸依旧精确
- **附件撤销**：空输入框按 `⌫` 撤销最后一张（逐张回退、每步发通知）、`⌥⌫` 一键清空，
  另有 `/image clear` 命令兜底。`TuiStore` 新增 `removeLastPendingImage()` 与
  `clearPendingImages()`；编辑器有文字时 ⌫ 保持原编辑语义，不会误删
- **`/image` 空参改为明细面板**：列出已附加图片并提示撤销手势（无附件时保持用法告警）；
  托盘不放常驻按键提示——说明集中在明细面板与 `/help`
- `/help`、命令菜单描述、README 同步更新；版本号升至 0.9.0

## [0.8.0] - 2026-08-27

### 设置持久化与 /config 命令

- **启动默认权限模式改为「自动允许」**：新增设置文件 `$DSH_HOME/fx-tui-settings.json`
  （`{ version: 1, approvalMode }`），缺文件或损坏时按内置默认 `auto` 起步——删除该
  文件即重置回默认
- **`/config` 内置命令**（别名 `/setting`、`/settings`）：无参数弹出交互选择器
  （标题带当前值，仿 /model）；支持 `/config permission <auto|ask>` 直改（亦接受
  中文词「自动允许 / 每次询问」）。修改同时持久化启动默认并立即对当前会话生效；
  非法用法回显用法提示。`/status` 的权限行拆为「当前会话 · 启动默认」两段，
  `/help` 收录该命令
- Shift+Tab 语义明确化：仍为会话级切换、从不写设置文件——避免临时的一次性切换被
  永久记住；`TuiStore` 新增 `initialApprovalMode` 构造参数与 `setApprovalMode()`
  方法，`cycleApprovalMode()` 复用之

## [0.7.0] - 2026-08-27

### 权限模式（Claude Code 式）

- **Shift+Tab 循环切换** `每次询问 ⇄ 自动允许`：输入框正下方常驻模式提示行
  （询问态灰字 `权限模式：每次询问（shift+tab 切换自动允许）`，自动态黄字
  `⏵⏵ 自动允许模式已开启`）。自动模式下 `approval/request` 直接放行、不再弹审批卡，
  会话级生效（每次启动回到"每次询问"），切换时在会话留一条通知便于审计；
  allowlist 记忆两种模式下照常生效
- `/help`、`fx --help`、`/status` 同步收录 Shift+Tab；并修正失实文案——帮助里
  原写 `/permission 切换权限模式`，但安装的 dsh 包并无该命令，模式切换即 Shift+Tab

### 修复：状态栏与横幅信息重复

- 状态栏右侧不再显示模型名与会话 id（与顶部横幅完全重复），只保留横幅没有的
  动态信息：`上下文水位 · token 用量`

### 修复：模式切换/新消息导致输入框逐行上移

- 弹性填充区的高度估算从"每项固定 +1 的保守偏置"改为**精确计算**：新模块
  `ui/ink-text.ts` 用 Ink 同款换行参数（`wrap-ansi {trim:false, hard:true}`）
  在视图实际换行宽度上算行数。此前每条通知实际渲染 1 行却按 2 行扣减预算，
  整帧随之每次矮 1 行——切 6 次模式输入框上移 6 行、底部多出 6 条空行
- 同类隐患一并修掉：用户消息按换行后行数计、工具卡片按视图真实截断/封顶计数
  （此前把未截断的完整输出全算进去）、审批卡/问题卡（含计划 30 行截断）逐项定高、
  输入框长行按换行后行数上报（粘贴超长行不再挤动布局）
- 回归实测：切 5 次模式（5 条通知）与发普通 + 超宽消息后，整帧行数 39 恒定不变

### 修复：启动后鼠标上滑出现整页空白

- 启动光标定位从"写入一整屏换行符下推"改为**光标归位（`\x1b[H`）+ 首帧自顶向下
  覆写**：首帧恰比视口矮一行（预留行），写入全程零滚动，不再把一页空白推进
  scrollback——鼠标上滑直接看到启动前的 shell 历史（新标签页则无内容可翻，
  Claude Code 式）。会话增长后的自然滚动不受影响
- `/edit` 外部编辑器路径保留换行下推：把屏幕上可见的会话推入 scrollback 保存，
  编辑器退出后不丢
- 实测（pty + 终端模拟器）：启动零溢出滚动、scrollback 0 行、横幅第 1 行、
  模式行第 39 行，八项断言全通过

## [0.6.1] - 2026-08-27

### 修复：横幅视口填充改为弹性空白

- **对话后横幅不再消失**：0.6.0 把撑满视口的空白在首帧一次性写死进 Static 区，
  任何新消息都会把整个画面上滚、横幅随即出屏，而中间的空白纹丝不动（对话一轮即消失）。
  现在空白改到**动态区顶部逐帧重算**（`行数 − 横幅 − 已落定内容 − 底部区域`）：
  会话不足一屏时新消息先消耗空白——横幅钉在屏幕顶缘、输入框钉底、**零滚动**
  （Claude Code 式）；空白耗尽后才自然滚动，横幅逐行没入 scrollback
- 配套：`InputBox` 向 `App` 上报精确渲染行数（编辑器行数/菜单/图片提示），
  供 filler 预算吸收布局变化；`/sessions` 切换与 `/edit` 外部编辑器返回后，
  横幅作为新 Static 批次的首条目重新打印（会话 id/模型随 `reset` 更新）
- 实测（pty + 终端模拟器）：初始/短对话零滚动/长对话渐进出屏/补全菜单零滚动/
  resize 重钉底/40 列降级，六场景全通过

## [0.6.0] - 2026-08-27

### 启动横幅（welcome banner）

- 进程启动时在终端顶部渲染 ASCII 品牌盒（Claude Code 式）：圆角 box-drawing 边框 +
  ANSI-shadow `FX` logo + 信息区（模型 / `fx-tui`+`dsh` 版本 / 会话 / 工作目录 / 提示行），
  取代原启动 notice 一行灰字
- dsh 版本号从已安装的 `@deepseek-ai/dsh-agent` package.json 读取（展示用途，读取失败则省略）
- 横幅是 Static 区第一个 transcript 条目：随对话自然滚入 scrollback，`--resume` 恢复会话
  时标注"已恢复历史会话"
- 全新会话首帧用空白填充撑满视口：横幅落在屏幕顶缘、输入框钉底（Claude Code 式
  启动画面）。填充预算为 Ink 追加在非全屏帧末尾的换行符预留一行（Ink 只按动态区
  高度判断全屏，横幅+输入区远小于视口时必然多出该换行，会把首帧整体上顶一行，
  正是"横幅顶边框滚出视口"的成因）；恢复会话有历史时不填充，由历史自然撑满
- 窄终端降级：先截断信息值（预留省略号宽度，保证不换行），空间不足再隐藏 logo 只留信息区；
  盒子宽度与底部输入框对齐——同为终端全宽，顶部横幅与底部输入区边框同宽，视觉齐整

## [0.5.1] - 2026-08-27

### 修复

- 补全菜单（/ 命令、@ 文件）改为**固定高度**：菜单打开期间始终占满 8 个条目槽位
  （过滤时用空行填充）。此前输入过滤会让菜单变矮、动态区帧收缩，输入框因此上浮。
  现在打字全程帧高不变，输入框钉底稳定（帧分析实证：过滤各步 cursorUp 恒定）。
  菜单内回车执行时，关闭与命令结果在同一个渲染提交中原子应用，无中间跳变。

## [0.5.0] - 2026-08-27

### 终端工程打磨（M5）

- 防闪烁：启用 Ink `incrementalRendering`（只重绘变更行），流式输出/菜单/Static 追加全量回归
- 滚动边界：动态区全部列表高度上限（菜单 8 / TODO 8 / plan 30 / 排队消息 5）
- `/status` 运行状态面板：版本/模型/会话/上下文/工作区 + 已加载插件树（registry 枚举）
- Windows 防御代码（编辑器 spawn 走 shell、kitty release 事件守卫；未实测，文档如实标注）
- IME 文档化：pty 内可自动化部分全链路回归，真机矩阵列为用户验证项

### 直达命令与输入体验

- `bin/fx` 透传启动脚本：装进 PATH 后 `fx` 直接启动（参数透传，`fx --resume <id>` 可用），
  含防呆检测——dsh 未安装或 fx profile 未装时输出可操作的修复命令而非裸报错
- 输入框去掉常驻占位提示：默认单行光标，仅输入超行时自然增高（消除两行→一行的跳动）

## [0.4.0] - 2026-08-27

### 会话与多任务（M4）

- `/sessions` 会话选择器 + **活体切换**：flush → dispose 当前 agent → resume 目标会话 → 状态重置与历史重放
- `/model` 模型切换：选择器热切换（改 `ModelSelectionRef.current`，下一步请求生效），
  并经 `agentDefaultModel.saveSelection` 持久化为默认
- 排队输入（steering）：忙碌时提交显示 `⏳ 已排队`，inbox 唤醒消费后提升为正式消息；中断丢弃并提示
- TODO 面板：`todo/write` 快照实时渲染（◐ 进行中 / ☐ 待办 / ☑ 完成 + 完成计数）
- subagent 徽标：`agent/created`/`agent/disposed` 按 parentSession 过滤，状态栏显示 `🌱×N`
- Plan 审批卡片：计划模式（`/plan`）下 `exit_plan_mode` 提交的计划经 plan-review 意图
  渲染为品红审批卡——计划 markdown 结构化展示、批准选项 `✓` 绿色标注、Enter 直接批准
- `/export` 会话导出为 Markdown 时间线
- `Ctrl+R` Transcript 模式：显示注入的隐藏上下文（plugin 快照、技能目录等）
- `/permission` 权限模式切换（dsh 注册表命令，随 slash 菜单自动并入）

### 体验修复

- 输入框钉底：启动时一次性行冲刷把首帧推到屏幕最底行（Claude Code 式），之后由自然滚动保持
- 命令反馈面板化：`/help` 与命令结果从一行灰字改为青色边框面板（修复"看起来没反应"）
- kitty 键盘协议 release 事件守卫：防止 kitty/WezTerm/Ghostty 下按键双触发

## [0.3.0] - 2026-08-27

### 输入效率（M3）

- Slash 命令补全菜单：`/` 弹出（内置 + dsh 命令注册表自动并入 /compact、/feedback、
  /goal、/permission…），↑↓ 导航、Tab 补全、Enter 执行（`ctx.commands.execute` 分发）
- @-文件引用：`@` 弹出工作区 fuzzy 路径补全（零依赖自研评分器，basename/前缀加权，
  30s 缓存跳过 node_modules/.git 等），Tab 补全相对路径，模型自动读取该文件
- `/edit` 外部编辑器：挂起 Ink → 打开 `$EDITOR` → 编辑内容回填输入框
- `/image <路径>` 图片附加：经 `ctx.attachments.saveImages` 持久化，ImageBlock 随下一条
  消息发送；配视觉路由（deepseek-v4-flash-vision-exp）验证了完整多模态闭环
- 修复：`readdir recursive` 返回绝对 parentPath——剥回工作区相对路径

## [0.2.0] - 2026-08-27

### 工具可见性与审批（M2）

- 工具卡片接入官方呈现层（`presentCall/presentResult`）：terminal 卡（耗时 + 退出码 pill）、
  diff 卡（LCS 红绿着色 + 路径头）、search/read/web 卡
- `Ctrl+O` 工具详情 摘要⇄完整 切换
- 审批四选项（一次/会话/总是/拒绝）+ 语义签名记忆（bash 按命令、fs 按路径），
  `a` 持久化到 `$DSH_HOME/fx-tui-allowlist.json`，同类调用自动放行
- agent 提问答 UI：`ctx.userQuestions.registerProvider`——选项卡（数字键多/单选）与
  自由文本两种模式；patch 挂载 `dsh-tool-ask-user` 提供 `ask_user_question` 工具
- 上下文水位：`tokenMeter.measure` + `request/context` 容量 → 状态栏 `上下文 N%·已用/容量`，
  窄终端右侧按优先级降级
- 修复：chunk 提交路径绕过自由文本问答分支、StatusBar columns=0 防御、
  `ctx.userQuestions` 需要 inject 声明

## [0.1.0] - 2026-08-27

### 核心回路（M1）

- 首个可用版本：dsh 树外 bundle（`dsh.bundle` manifest + `cordis.patch.yml` 叠加 dsh-base）
- 多行输入框：码点级光标编辑、Enter 提交 / Ctrl+J 换行、输入历史、bracketed paste 通道、
  块输入规范化
- 流式 Markdown 渲染（marked + cli-highlight + wrap-ansi，CJK 感知）、状态栏
  （阶段/思考字数/token 用量）、工具调用卡片、`--resume` 会话恢复、Esc 中断、
  双击 Ctrl+C 退出、`/help` `/exit` 内置命令
- 端到端验证于 dsh 0.1.1-rc.2：真实模型回路、工具执行、中断、恢复

[0.9.0]: https://github.com/xianglifei/fx-tui/releases/tag/v0.9.0
[0.7.0]: https://github.com/xianglifei/fx-tui/releases/tag/v0.7.0
[0.6.1]: https://github.com/xianglifei/fx-tui/releases/tag/v0.6.1
[0.6.0]: https://github.com/xianglifei/fx-tui/releases/tag/v0.6.0
[0.5.1]: https://github.com/xianglifei/fx-tui/releases/tag/v0.5.1
[0.5.0]: https://github.com/xianglifei/fx-tui/releases/tag/v0.5.0
[0.4.0]: https://github.com/xianglifei/fx-tui/releases/tag/v0.4.0
[0.3.0]: https://github.com/xianglifei/fx-tui/releases/tag/v0.3.0
[0.2.0]: https://github.com/xianglifei/fx-tui/releases/tag/v0.2.0
[0.1.0]: https://github.com/xianglifei/fx-tui/releases/tag/v0.1.0
